import LoginFormContainer from '../../components/auth/login/loginFormContainer';

const LoginPage = () => {
    return (
        <div>
            <LoginFormContainer />
        </div>
    )
}

export default LoginPage;